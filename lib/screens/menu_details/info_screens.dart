import 'package:flutter/material.dart';

class InfoScreens extends StatelessWidget {
  const InfoScreens({
    super.key,
  });
  // final int test;

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Giới thiệu")),
    );
  }
}
